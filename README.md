# cx8_multiplane_manager
Bash tool to auto-manage ConnectX-8 SMI devices and OpenSM instances: discovers CX8 mlx5 parents, creates canonical smiN devices, launches one opensm per Port GUID, verifies port states, and logs every phase for easy debugging. 
